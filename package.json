{
  "name": "20160319",
  "version": "0.0.0",
  "description": "Docker Wuhan MeetUp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodeppt start -p 8080 -d ./build/",
    "execti": "docker exec -ti MeetUp /bin/bash",
    "build": "gulp include && nodeppt generate ./build ./dist -a",
    "ghpages": "gh-pages -d ./dist"
  },
  "author": "FooTearth <footearth@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "gulp-file-include": "^0.13.7",
    "build-module-task": "^0.0.2",
    "gulp": "^3.9.1",
    "run-sequence": "^1.1.5",
    "coffee-script": "^1.10.0",
    "gulp-rename": "^1.2.2"
  }
}
